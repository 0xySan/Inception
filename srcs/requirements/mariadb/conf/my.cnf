[mysqld]
user        = mysql
pid-file    = /run/mysqld/mysqld.pid
socket      = /run/mysqld/mysqld.sock
datadir     = /var/lib/mysql
symbolic-links=0

# InnoDB settings for small container
innodb_buffer_pool_size = 64M
innodb_log_file_size    = 32M
innodb_flush_method     = O_DIRECT